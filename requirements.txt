# CAPM AI Study Agent - Python dependencies
#
# All tools use only the Python standard library (json, argparse, random,
# datetime, pathlib, collections) — no third-party packages are required
# to run tools/*.py.
#
# This file is kept for future extensions (e.g., a richer CLI/TUI,
# data visualization of progress, or a web dashboard) and to make the
# project's dependency footprint explicit.

# Example future dependencies (uncomment if added):
# rich>=13.0.0        # nicer terminal output for quizzes
# tabulate>=0.9.0      # formatted score tables
